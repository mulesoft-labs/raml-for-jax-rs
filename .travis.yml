language: java
sudo: false
after_success:
    - "[ ${TRAVIS_PULL_REQUEST} = 'false' ] && [[ $TRAVIS_BRANCH =~ (master|^release/[0-9]+.[0-9]+.[0-9]+) ]] && mvn -Dusername='$NEXUS_USER' -Dpassword='$NEXUS_PASSWORD' deploy"

apply plugin: 'java'
apply plugin: 'jaxrstoraml'


group 'org.raml'
version '2.0.1-SNAPSHOT'


buildscript {
    repositories {
        mavenLocal()
        maven { url 'https://repository.mulesoft.org/snapshots/' }
        maven { url 'https://repository.mulesoft.org/releases/' }
        mavenCentral()
    }
    dependencies {
        classpath "org.raml:jaxrs-to-raml-gradle-plugin:2.0.1-SNAPSHOT"
    }
}

repositories {
    mavenLocal()
    maven { url 'https://repository.mulesoft.org/snapshots/' }
    maven { url 'https://repository.mulesoft.org/releases/' }
    mavenCentral()
}

project(':jaxrs-to-raml-annotations-annotations') {

/*
    raml {
        inputPath = file(buildDir)
        outputDirectory = file(buildDir)
        sourceDirectory = file(sourceSets.main.java.srcDir)
    }
*/
}

clean {
    subprojects
}